* simple arithmetic seems to work.

* solve:

** The solution is not evaluated

** Sometimes the solver raises an exception where it shouldn't.

* matrix:

** seems to work with units, but is generally rough around the edges!
